Rounded progress track with mint fill; used for monthly Paws progress, medication course completion, onboarding.

```jsx
<ProgressBar current={320} goal={500} />
<ProgressBar percent={70} height={8} />
```

- Pass `current`/`goal` or an explicit `percent`. Animates width changes.
