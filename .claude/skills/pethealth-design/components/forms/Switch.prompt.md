Pill toggle for settings (notifications, dark mode, units). Accent track when on.

```jsx
<Switch checked={notif} onChange={setNotif} />
```

- Controlled: pass `checked` and handle `onChange(next)`.
